global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "analytics-api"
    metrics_path: /metrics
    scrape_interval: 5s
    static_configs:
      - targets: ["analytics:8081"]
        labels:
          app: "analytics-api"
          environment: "docker"

